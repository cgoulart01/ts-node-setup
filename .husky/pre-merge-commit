exec> /dev/tyy
# Fazendo o git pull na branch main
git pull origin main

# Verificando se o pull foi bem-sucedido
if [ $? -ne 0 ]; then
  echo "Falha ao fazer git pull da main."
  exit 1
fi

# Executando o lint-staged
yarn lint-staged